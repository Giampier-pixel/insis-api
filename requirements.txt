# ============================================================
# INSIS API — Dependencias de producción
# ============================================================

# Framework
Django~=5.2.0
djangorestframework==3.16.*

# Auth JWT
djangorestframework-simplejwt==5.5.*

# Documentación OpenAPI
drf-spectacular==0.29.*

# Tareas asíncronas
celery[redis]==5.6.*
django-celery-beat==2.9.*

# Base de datos MySQL
mysqlclient==2.2.*

# Configuración
python-decouple>=3.8

# Filtros
django-filter==25.*

# Reportes Excel
openpyxl>=3.1.5
google-cloud-storage>=2.19

# Image processing (avatars)
Pillow>=11.0

# Servidor WSGI
gunicorn==23.0.*

# Static files (producción — WhiteNoise sirve estáticos sin Nginx)
whitenoise>=6.8

# CORS headers (permisos cross-origin para el frontend)
django-cors-headers>=4.6

# Redis client (también requerido por celery[redis], explicitado para entrypoint)
redis>=5.0
